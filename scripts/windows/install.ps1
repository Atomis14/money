# https://flask.palletsprojects.com/en/2.1.x/installation/
# run "Set-ExecutionPolicy RemoteSigned" in admin powershell before this script

py -3 -m venv venv      # install venv

pip install Flask       # install Flask